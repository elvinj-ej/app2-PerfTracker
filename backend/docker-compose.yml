services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: aose
      POSTGRES_PASSWORD: aose
      POSTGRES_DB: aose
    ports:
      - "5432:5432"
    volumes:
      - aose_pgdata:/var/lib/postgresql/data

volumes:
  aose_pgdata:
