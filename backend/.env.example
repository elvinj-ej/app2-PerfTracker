# Default: a local SQLite file, no separate database server needed.
DATABASE_URL=sqlite:///./perftracker.db
# To use Postgres instead, uncomment and adjust:
# DATABASE_URL=postgresql+psycopg://perftracker:perftracker@localhost:5432/perftracker

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5
CORS_ORIGINS=http://localhost:5173
STATIC_DIR=static
